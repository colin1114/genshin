#include <bits/stdc++.h>
#define endl '\n'
#define ll long long

#define PII pair<ll, ll>
#define x first
#define y second

using namespace std;
const int E = 1e6 + 5;
ll n, m;
struct node
{
    ll l, r, a, b;
} a[E];
int main()
{
    cin >> n >> m;
    for (int i = 1; i <= m; i++)
        cin >> a[i].l >> a[i].r >> a[i].a >> a[i].b;
    for (int i = 1; i <= m; i++)
    {
        
    }
        return 0;
}